Good investigation, the BrandAudienceSegment mirror at generate-microsite.ts:791 was a real catch — yes, add micrositeBlockList? to both the frontend AudienceSegment and the API mirror, otherwise the prompt builder won't see the field. And your slug diagnosis is right: the leak is the prompt input (audience echoed at 1204/1251), not a server-side concat. My spec was wrong to talk about a "slug generator" — there isn't one for this.
Decisions on the three open questions:
1. Slug fix → option (a) with a server-side guardrail. Feed segment.name (human label) into the prompt at 1204 and 1251 instead of the id. Keep the slug AI-authored. The reason for (a) over (b): the AI is already doing a reasonable job composing contextual slugs (zoom-salesforce-...), and going deterministic loses that judgment with no clear payoff. The empirical bug is "raw enum leaked into prompt context"; fix that and the symptom goes away.
Add one belt-and-suspenders guardrail though: after parsed.slug is sanitized at 1278, also strip any segment.id substring if it appears (regex against the active segment's id only — don't blanket-strip all segment ids). If the AI ever glitches and echoes the id from somewhere else we haven't found, the slug still comes out clean. Log a warning when the strip fires so we know if it ever triggers.
Also: grep one more time for any other prompt site that passes the raw audience enum into the model. Lines 1204 and 1251 are the ones you found; if there are others, fix them too.
2. schemaHint storage → verbatim, yes. Store the full strings including the inline guidance ("— 4 DSO pain points specific to this account", "— closing call to action", "— highlight AI Scan Review", etc.) in Dandy's seeded segments. The inline guidance is segment-specific tuning, not pure schema — even with DRY referencing it would still need to live somewhere segment-specific, so the DRY win is mostly imaginary. Verbatim storage gives the regression guarantee.
Confirm the optional fallback works as: if a tenant's micrositeBlockList[i].schemaHint is omitted, fall through to BLOCK_PROP_SCHEMAS[type] (registry default). If the type isn't in the registry either, fall through to the existing "{ ...fields }" placeholder. Three-tier fallback, no crashes on missing data.
3. Dandy seed → one-off data migration script, but check first. You're right that there's no checked-in brand seed — brand config is per-tenant JSON in the DB, populated through Brand Settings UI. So Step 5 is a one-off idempotent admin script:

Read first, report back. Before writing anything, fetch Dandy's current brand.segments and show me what's already there. The per-account accountSegment matching flow uses the same array — Dandy may already have segments populated (or partially populated) from that workflow. I need to see the current state before any merge.
Merge by id, don't overwrite. If a segment with id dso-corporate / dso-practice / independent already exists, merge the new fields (micrositeBlockList, any messaging tweaks) into the existing entry without clobbering anything the team has hand-tuned. If those ids don't exist, insert them. Don't touch any other segment ids.
Idempotent. Re-running the script does nothing if Dandy already has the three segments fully seeded.
Tenant-scoped explicitly. Hardcode Dandy's tenant id (or look it up via PROTECTED_ENTERPRISE_SLUGS — slug dandy is in that protected list per @workspace/plan-config). Don't write to anyone else's brand.

Show me the script + a dry-run diff of what Dandy's brand.segments would look like before vs. after, then I'll OK the actual run.
Otherwise the architecture is approved as you've laid it out. Proceed with the audit-confirmed plan, ship Steps 1–4 and the seed script together (gated on the dry-run review), and do the test plan from the prompt before considering it shipped.