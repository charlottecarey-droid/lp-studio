- Env/.env handling — [dev env source](dev-env-source.md): workflows read `.replit [userenv]` + gitignored `.env`; Replit Secrets NOT injected into workflow shells · [prod ships dev .env](prod-env-dev-dotenv-clobber.md): blank dev `.env` keys clobber injected Secrets in prod → boot crash-loop; gate `.env` load on NODE_ENV.
- api-server dev dist — [stale dist](api-server-dev-stale-dist.md): dev = build+start (no watch), serves stale dist after a merge until restarted; restart, don't hunt a code bug · [e2e runButton restart race](e2e-runbutton-restart-race.md): runButton-ran e2e rebuilds the SHARED dist → preview crash; fix=removeWorkflow(e2e).
- [pnpm overrides location + range](pnpm-overrides-location-and-range.md) — overrides live ONLY in pnpm-workspace.yaml; never add a package.json pnpm.overrides block (silently clobbers every workspace pin/prune); bump a pinned dep at its pin; `^` not `>=` for toolchain-deep deps.
- Deployment gotchas — [image 8 GiB trim](deployment-image-size-trim.md): append `rm -rf` (.pnpm-store/.config-chromium/attached_assets) to artifact production.build; keep .cache + node_modules · [Resend from stale deployment](resend-from-stale-deployment.md): 403 naming an old domain = stale deploy env (snapshots at boot); republish, not code.
- [Git merge conflicts as main agent](git-merge-conflicts-main-agent.md) — agent can't write under `.git/` (no add/commit/rm/reset/fetch); resolve working tree (strip markers, pick ONE side per interdependent rewrite) + validate; user runs git + clears stale `.git/*.lock` via Shell.
- [Workspace behind GitHub reconcile](workspace-behind-github-reconcile.md) — push-rejected/divergent when GitHub is AHEAD of the workspace: reset --hard origin + cherry-pick the workspace-only fix + FF push (never force); verify fixed file's base unchanged first; stale origin/HEAD.lock nags fetch only.
- AI image/icon sanitize — [trust-bar numeric-only](ai-trust-bar-numeric-only.md): skip STAT_BAR_BLOCK_TYPES at every assign/restore · [icon URL hard-strip](ai-icon-url-hard-strip.md): blank URL-shaped icons across ALL keys · [icon-only item photos](icon-only-item-photos-sanitize-strip.md): strip items[].image unless useItemPhotos · [customer-logo slots](customer-logo-slot-no-fill.md): exclude logo boxes from slot collect/fill, not sanitize.
- [Modal theme fallback callsites](modal-theme-fallback.md) — BrandConfig modal fallback must happen at every EmailCaptureModal callsite; the modal itself has no brand context.
- [EmailWYSIWYGEditor mergeVars contract](email-wysiwyg-mergevars-bare.md) — editor mergeVars/insertMergeVar need BARE names (it wraps {{…}} itself); wrapped/empty values cause the empty-{{}} bug; plain-text chips keep wrapped tokens.
- [Workspace login branding](workspace-login-branding.md) — tenant logins must read as LP Studio (logo+wordmark), Dandy is the sole white-label exception; no custom tenant logo, no "powered by".
- Marketing site UI — [dev marketing host](lp-studio-dev-marketing-host.md): dev '/' = marketing site; SaaS shell via '/?preview=app' · [banner+nav scroll coord](marketing-banner-nav-scroll-coordination.md): two fixed bars share `--lp-banner-h`; same threshold + equal durations, never zero the var on scroll · [hero gradient-text vanish](marketing-hero-gradient-text.md): gradient-on-transparent text needs `.hero-gradient-text` w/ solid fallback or the line intermittently vanishes.
- [LP_PUBLIC auth hydration](lp-public-auth-hydration.md) — auth-conditional logic on LP_PUBLIC routes needs `optionalAuth` middleware or `req.authUser` is always undefined.
- [Tenant shell vs marketing-flash band-aids](tenant-shell-vs-marketing-flash.md) — fixing tenant-host flash requires serving a different shell from the CF worker; inline "hide #root" scripts in index.html are NOT a real fix.
- Stripe infra — [apiVersion pinning](stripe-api-version-pinning.md): copy the literal from stripe/esm/apiVersion.d.ts after SDK bumps · [dunning order](stripe-dunning-email-ordering.md): downgrade FIRST, then best-effort send · [accounts self-heal gating](stripe-accounts-selfheal-gating.md): gate boot heal on tracker 0050 · [test mode in dev](stripe-test-mode-dev.md): live keys are GLOBAL Secrets; remap STRIPE_SECRET_KEY_TEST in loadEnv dev-only branch · [Checkout E2E test clock](stripe-checkout-e2e-testclock.md): drive signed events through stripeWebhookHandler; hand-built events MUST include `created`.
- CF edge routing — [worker routes source of truth](cf-worker-routes-source-of-truth.md): never `routes=[...]` in wrangler.toml; use sync-worker-routes.ts · [Dandy microsite cutover](dandy-microsite-cf-cutover.md): CNAME→lpstudio.ai; CNAME forces dropping per-host replit-verify TXT; verify via DoH · [Dandy app domains bypass CF (OG)](dandy-app-domains-bypass-cf-og.md): domains on Replit static skip the worker → wrong OG card; fix = route through CF (INFRA).
- AI copy voice — [always-inject](ai-copy-voice-always-inject.md): brand voice + active brief on every AI copy call, never gated on a colors/fonts toggle · [segment = additive not override](ai-segment-hierarchy-priority-not-override.md): brand = constant foundation, segment = additive emphasis; any priority framing starves depth → thin copy.
- [AI reasoning model empty output](ai-reasoning-model-empty-output.md) — gpt-5-* reasoning models under a tight max_completion_tokens return EMPTY content (silent); standard is gpt-4o (copy/SEO) + gpt-4o-mini (cheap JSON/vision extractors), NEVER gpt-5-mini — a bulk "upgrade" to it silently broke brand-import + image-tagging.
- [AI copy brand-voice system anchor](ai-copy-brand-voice-system-anchor.md) — generic/identical-across-tenants full-page copy = brand voice only in USER prompt; fix = anchor it in the SYSTEM prompt too (additive, copyExamples promoted into it), never churn buildBrandContext or widen critique.
- Page recipes — [generate-page randomness](generate-page-determinism-sources.md): 4 randomness knobs; seed via lpHashSeed; keep pickRecipe all-excluded fallback · [shared-prompt no brand-leak](recipe-shared-prompt-text-no-brand-leak.md): recipe wording injects into ALL dental tenants' prompts; keep brand-neutral · [wording builder](page-recipe-wording-editor.md): superadmin tab = full builder via page_recipe_overrides; skeleton OR-alternatives need uppercase ` OR ` · [freestyle off-topic](recipe-freestyle-override.md): off-topic asks discard the rotated sales recipe; clause in 3 synced spots.
- [Legacy default-on boolean config](legacy-default-on-boolean-config.md) — new default-ON BrandConfig flags must be read as `!== false` at every callsite; truthy checks silently flip legacy rows to OFF.
- Drizzle migration journal skips — [journal must list every tag](auth-exchange-codes-schema.md): a .sql without its tag in meta/_journal.json is silently skipped every release · [drifted-DB high-water skip](notification-migration-skipped-on-drifted-db.md): tags journaled below the high-water mark skip forever; self-healed in migrate.ts, dispatcher rethrows 42P01/42703.
- Backend security — [fetch redirect SSRF](fetch-redirect-ssrf.md): untrusted fetches use redirect:"manual" + re-validate each hop's host · [config URL→href allowlist](marketing-config-url-href-allowlist.md): admin-editable URLs in public hrefs need a server-side http(s) allowlist · [tenant-scoped queries fail closed](tenant-scoped-query-fail-closed.md): required tenantId or return empty; never unfiltered/`limit(1)` fallback.
- Page head meta/title — [OG/SEO pipeline](og-meta-pipeline.md): scrapers read pre-baked HTML; a managed tag = 3 coordinated edits · [app-shell tab title](app-shell-tab-title.md): authed tab title via AppLayout/resolveAppPageName; the page's own title effect wins · [default favicon assets](lp-studio-default-favicon-assets.md): SVG + 5 raster fallbacks, regenerate ALL together; Dandy is a main.tsx runtime swap.
- [Marketing-site superadmin config baking](marketing-site-config-baking.md) — to make a marketing value editable AND reach scrapers: DB+public endpoint, home.tsx global+fetch w/ field-fallback, prerender best-effort `pg` read (NOT @workspace/db) + addInitScript global.
- Plan/trial state — [tier model](stripe-tier-model.md): cancel→free; Checkout only if no active sub else Portal; untrusted price fails closed · [live accessor](plan-config-live-accessor.md): getPlanFeatures+getTenantPlan, not static PLAN_FEATURES · [no plan='trial' write](trial-plan-write-paths.md): date-window only · [free closes trial](plan-free-write-trial-close.md): splice CLOSE_TRIAL_ON_FREE_SQL or effectivePlan re-lifts the trial.
- [LP hero overlayOpacity scale](lp-hero-overlay-opacity-scale.md) — LP hero overlayOpacity is a 0-100 percent (renderer ÷100); AI prompt schemas must use whole-number percents, not 0-1.
- [Composite lib dist drives types](lib-db-composite-dist-types.md) — editing a composite lib schema → consumer reads stale dist → phantom "property does not exist"; fix with `tsc -b <lib> --force`. See one-pager-types-composite-dist.md, plan-config-composite-dist.md, composite-lib-test-exclude.md.
- Block contrast guards — [generated block contrast](generated-block-contrast.md): derive text/badge/button colors from actual bg, fix at render-block layer · [preset-style CSS-var bypass](lp-preset-style-bypass-contrast-guards.md): resolve preset→hex BEFORE contrast math · [block brand-var surface bypass](block-brand-var-surface-contrast.md): resolve var()→live brand hex before contrast math · [contrastTextColor mispick](contrast-text-color-mispick.md): CTA labels use pickContrastingColor(…,4.5).
- [Prod DB is Neon not Helium](prod-db-is-neon-not-helium.md) — app uses NEON_DATABASE_URL; executeSql dev/prod targets read stale Helium DB. Verify prod by querying Neon directly; dev-shell scripts via lib/db hit PROD.
- [R2 boot sweeps vs startup probe](r2-boot-sweeps-vs-startup-probe.md) — build-OK-but-promote-failed w/ zero logs = startup-probe starvation; keep R2 sweeps off app.listen path; all S3 clients via buildR2S3Client (200 sockets).
- Content-series — [guest fields](content-series-guest-fields.md): podcast RSS has no guest field; populate via AI prose extraction, hero sources guest from the targeted episode alone · [brand accent/logo injection](content-series-brand-accent-logo-injection.md): inject accentColor (as theme.primary) + logoUrl via applyContentSeriesBranding; non-logo brands keep text-logo fallback.
- [Block role tags](block-role-tags.md) — semantic tag vocab+defaults live ONLY in lp-template-engine/block-tags.ts; DB overrides via resolveBlockTags; AI guide parses allowed blocks from system-prompt `- "type":` bullets (silently empties if format changes).
- One-pager generators (sales) — [brand asset URLs](one-pager-brand-asset-urls.md): static public files; root-relative need base-path norm · [LP-viewer framing](lp-viewer-onepager-framing.md): detect via `one-pager-hero` block · [heading font](one-pager-heading-font-resolution.md): via getFontList(), never ensureBagoss outside Agreement · [body-control parity](one-pager-body-control-parity.md): a slider no-ops unless THAT template reads the field · [partner paths](partner-onepager-pdf-only.md): renders BOTH jsPDF + web one-pager.
- inject() route tests — [in-process inject](vitest-listen-hangs-inproc-inject.md): vitest pool never fires app.listen; use inject() not port+fetch · [rate-limit key](rate-limited-route-inject-tests.md): trust proxy 1 + unique X-Forwarded-For per call · [post-response side-effects](inject-test-post-response-sideeffects.md): fire-and-continue work needs a bounded poll after inject resolves.
- [E2E Dandy-gating both paths](e2e-dandy-tenant-limit.md) — client gate=brandName (royal fixture w/ brandName:"Dandy"), server gate=slug (impersonate seeded dandy-smb + cleanup rows); both paths testable.
- [Chili Piper booking tracking](chili-piper-booking-tracking.md) — 2 surfaces (hero ChiliPiperButton + cta-button→ChiliPiperModal hook); never gate booking recording on lead PII; only `if(!url)return`; omit testId/variantId unless a real A/B test.
- [mockup-sandbox generated file untracked](mockup-sandbox-generated-file.md) — src/.generated/mockup-components.ts is gitignored (regenerated on dev start); never re-add, it conflicts every rebase.
- Neon pooler quirks — [advisory-lock leak](pooler-advisory-lock-leak.md): session pg_try_advisory_lock leaks on -pooler, prefer pg_advisory_xact_lock; clear orphans via pg_terminate_backend · [LISTEN needs non-pooled](notification-cross-instance-broker.md): SSE fan-out via LISTEN/NOTIFY; NOTIFY ok on pooler, LISTEN must strip `-pooler`; INSTANCE_ID skips own echo.
- Auth token gates — [email token host-binding](auth-email-token-host-binding.md): every redeem route re-checks `redeemed.targetHost` vs current host · [OAuth login-state nonce](oauth-login-state-csrf.md): callbacks redeem a server-stored single-use nonce before token exchange; test via the oauth_not_configured fall-through.
- jsPDF/PDF generation — [brand-font embedding](pdf-brand-font-embedding.md): magic-byte-validate before addFont; Google v1 /css + curl UA returns full-charset TTF · [fill-color encoding](jspdf-fill-color-encoding.md): fills=2-decimal `rg`, text=3-decimal; hardcode brand RGB literals.
- Platform email — [hard-fallback](system-email-hard-fallback.md): transactional/auth senders need a self-contained HTML fallback, never the editable shell · [token coordinated edits](email-token-coordinated-edits.md): new token = 4 synced edits or it leaks {{token}}/renders blank · [CTA always baked](lifecycle-email-cta-always-baked.md): authored body_html always emits the CTA.
- Long commands past bash 120s cap (detached procs get reaped) — [long e2e via workflow](running-long-e2e-via-workflow.md): temporary console workflow + poll getWorkflowStatus · [lp-studio typecheck](lp-studio-typecheck-via-validation.md): tsc >12min, run via the validation skill.
- [Tenant LP default-noindex](tenant-lp-default-noindex.md) — all tenant LPs noindex by default (Dandy slug-excepted), fail CLOSED; emit via robots <meta> + X-Robots-Tag in BOTH R2 prerender (x-robots customMetadata, both CF workers) AND live paths.
- Drizzle SQL quirks — [multi-statement extended protocol](drizzle-multistatement-extended-protocol.md): run whole .sql files via raw `pool.query(string)`; `db.execute(sql.raw(...))` → extended protocol → "cannot insert multiple commands" · [ANY(array) vs IN](drizzle-any-array-not-in.md): bare JS array = tuple, INVALID for `=ANY()`; use `sql\`ARRAY[...]::text[]\``.
- Notification templates — [channels stale row](notification-channels-stale-row.md): adding a channel never backfills existing rows → stale row hides editor; fix via data UPDATE · [DB-only templates](db-only-notification-templates.md): resolve via getNotificationTemplate, not NOTIFICATION_TEMPLATES[key], or 404s.
- SSE streaming — [E2E networkidle](e2e-networkidle-sse.md): persistent SSE keeps networkidle from settling; use domcontentloaded + element asserts · [proxy buffering keepalive](sse-proxy-buffering-keepalive.md): platform proxy buffers low-traffic SSE; needs a BIG ~16KiB ~1s padded keepalive; can't repro locally.
- Brand-import — [mirror vs vision](brand-import-mirror-decoupled-from-vision.md): read referenceImageUrls from RAW results too · [screenshot two sizes](brand-import-screenshot-two-sizes.md): full-res for vision, downsampled <5MB preview cached · [evidence timeouts](brand-import-evidence-timeouts.md): outer budget must DERIVE from sum of inner caps · [cache poisoning](brand-import-cache-poisoning.md): never cache all-failed; image recovery must BYPASS the (by-design) partial-success cache.
- Workflow scheduling/delivery — ["once" date fail-closed](workflow-once-date-failclosed.md): round-trip date components through Date.UTC; Date.parse normalises impossible dates → fires wrong day · [retry "deduped" ≠ delivered](workflow-retry-deduped-not-delivery.md): verify the occupying notification_sends row is status='sent' · [engine hard-fallback](workflow-engine-hard-fallback.md): MATCHED-but-non-executable workflow must still run the code-default fallback (gate on isWorkflowExecutable) · [welcome fan-out](welcome-email-workflow-fanout.md): duplicate welcome emails = extra operator workflows on the event; delete them, not the template.
- Full-page block wiring/classification — [classification two surfaces](fullpage-classification-two-surfaces.md): FULL_PAGE_BLOCK_TYPES + marker-guarded re-shelf; late additions need a NEW marker key · [full-page block wiring](lp-fullpage-block-wiring.md): mirror content-series at EVERY callsite incl. NO_REVEAL · [sticky NO_REVEAL](no-reveal-sticky-blocks.md): internally-sticky blocks MUST be NO_REVEAL or sticky breaks on published pages only.
- [Settings hub consolidation](settings-hub-consolidation.md) — Settings is ONE tabbed SettingsPage (owns AppLayout); section bodies are `*Content` exports; broadcast-recipient "default" = NO row (DELETE to restore, never save-empty).
- [Storage CORP for sandboxed previews](storage-corp-sandboxed-preview.md) — assets in sandboxed/opaque-origin iframes (email preview srcdoc) need `Cross-Origin-Resource-Policy: cross-origin` or logos silently break; test in the sandbox, not a normal load.
- Template seeding/gating — [global seed marker](global-template-seed-and-gating.md): new global templates need a `global_templates_seed_vNN` bump in migrate.ts; DB globals NOT industry-gated server-side · [ind- slug detection](industry-templates-null-tag-slug-detection.md): seeded industry starters carry industry:null, identified ONLY by `ind-` slug prefix; gallery/filter must use isIndustryTemplate (slug-based) or the section is silently empty.
- Published R2 snapshots — [provenance footer per-host](snapshot-provenance-footer-per-host.md): bake "Sent by" footer maximal then strip per host; keep marker+gate+prerender-host in sync · [render-version reconcile](snapshot-render-version-reconcile.md): self-heal via manually-bumped CURRENT_RENDER_VERSION + post-deploy reconcile; dev shares prod's R2 bucket — NEVER run the reconcile/backfill from dev.
- [Email preference center grouping](email-preference-center-grouping.md) — personal prefs grouped into fixed taxonomy; opt-out stored as ONE `grp:<id>` row (durable for future templates), checked inside isOptedOut; never per-template.
- [Sales layout-defaults save feedback](sales-layout-defaults-save-feedback.md) — "/api" is same-origin; CSRF auto-injected by a global window.fetch interceptor (curl 403 is expected). Real bug: write helper swallowed errors + no res.ok check → false "saved". Never swallow source-of-truth writes.
- [Tenant custom-html calculator edits](tenant-custom-html-calculator-edits.md) — calculator pages live in lp_pages.blocks props.html (DB, not repo); dump live block first, update Neon + republish, verify via /api/lp/page/:slug.
- Lead field handling — [tenant-authored field keys](lp-leads-field-keys.md): fields jsonb has no fixed schema; exact-key reads drop "Email Address"-style leads, normalize keys; empty dashboard can also = api-server down · [shared lead/test heuristic](shared-lead-heuristic.md): isTestLead/leadName/fieldAccessor live ONLY in @workspace/lead-utils; all 4 surfaces agree; filter is in-memory JS.
- [Launch-path DB reads in try](launch-path-db-reads-in-try.md) — bare "Failed to launch campaign" (no detail) = unhandled throw before the route try → non-JSON; every send-path DB read must be INSIDE the try + withDbRetry.
- Sales microsites/business cases — [account linkage & templates](sales-microsite-account-linkage.md): account→microsite via hotlinks+pageVariables+account_id cols (keep callsites in sync); business-case templates are global-only single-block monographs · [view-time var parity](business-case-viewtime-var-parity.md): {{company_name}}/{{practice_count}} fill at BOTH bake and view-time via shared businessCaseVars or tokens leak on /p/:token.
- [Imported button CSS marker](imported-button-css-marker.md) — buttonStyleRaw applies page-wide via `lp-brand-btn` + injected `!important` stylesheet; outline/secondary pass `{imported:false}`; resolveImportedButtonLabelColor must contrast-check scraped label vs resolved fill (>=3) or CTAs go invisible page-wide.
- [Conversion-scoring shape detection](conversion-scoring-shape-detection.md) — analyzeBlocks must detect hero/social/trust/booking/imagery by object SHAPE not exact prop names (premium/DSO blocks use bespoke names); keep narrow imageCount (speed) vs broad hasImagery (visual) split.
- [Edge WAF blocks email-HTML bodies](edge-waf-html-request-body.md) — prod-only minimal-HTML 403 (Cloudflare, no origin log) on POSTs carrying email HTML (`href="{{token}}"`); fix = base64-wrap JSON body (now GLOBAL in lp-studio api-fetch interceptor), decode in a global middleware.
- [generate-page nav/footer auto-injection](generate-page-chrome-injection.md) — AI-page gen force-injects nav/cta/footer server-side; prompt-only "don't add X" rules need a matching post-processing guard (isSingleFullPageBlock for self-contained full-page blocks).
- [Tenant-facing placeholder branding](tenant-facing-placeholder-branding.md) — shared app-UI input placeholders must be brand-neutral (Acme/example.com/yourcompany.chilipiper.com), never Dandy; scope de-brands to `placeholder=` attrs only, never value/default constants.
- Signup gates — [Turnstile secret-presence](turnstile-secret-presence-gating.md): global secret enables Turnstile in dev+e2e too; prod boot guard fails publish; pair SITE key + `TURNSTILE_SECRET_KEY:""` in playwright.config · [trial phone gate](trial-phone-gate.md): one-trial-per-SMS-phone fail-safe-OFF on twilioConfigured(); client E.164 echo safe (Twilio Verify gates it).
- [Workflow tick integration isolation](workflow-tick-integration-isolation.md) — testing runWorkflowTick on shared prod DB: far-future delay + drive only seeded rows (sweep is global → steals dispatch & can send real bucket members); seed admin bucket not superadmin.
- [Scheduled trigger timezone math](scheduled-trigger-timezone.md) — schedules carry optional IANA timezone (default UTC); dueOccurrenceId resolves all boundaries+fire instant in-zone, occurrence id = LOCAL date; invalid tz fails closed.
- Sales delete/undo — [synced rows deletable](sales-delete-synced-rows.md): synced rows hard-deletable but may resync; UI warns via amber note · [undo restore](undo-delete-restore.md): capture-and-reinsert via restoreRows (forced tenant/page scope, idempotent, preserves ids+timestamps).
- Template thumbnails (thum.io) — [capture](template-thumbnail-capture.md): gallery cards thumbnailUrl→ogImage→gradient, isPlaceholderTemplateLabel duplicated in templates.ts + backfill (deliberate prod job) · [output cap](thumio-output-cap.md): anonymous gateway caps ~1280px, width/2400 breaks ratio to square; max clean 3:2 = viewport/1600x1067/width/1600/crop/800 → 1280×853.
- LP URL/path microsite-aware — [published URL tenant host](lp-page-url-tenant-host.md): getLpPageUrl builds from user.tenantHost, never window.location.origin · [path display](lp-path-display-microsite-aware.md): microsite tenants serve at root; labels via micrositeDomain ternary, live links via getLpPageUrl.
- [Conversion-score metrics](conversion-score-pagespeed.md) — Page Speed prefers real PSI via TTL cache + non-blocking bg fetch (falls back to block-count; VITEST off); Conversions=leads+non-form events, CVR over visits; details also in conversion-score-conversions-metric.md.
- Tenant email sending — [footer postal address](email-footer-physical-address.md): each shell sources its OWN {{physicalAddress}}; ensureFooterAddress injects for custom footers · [default sender](tenant-default-email-sender.md): zero-setup {Brand}<slug@mail.lpstudio.ai> via resolveTenantSender; fail closed on unverified custom domain · [logo absolute URL](tenant-email-logo-absolute-url.md): root-relative stored logos must normalize to absolute via app public host or they break in mail clients.
- Sales data ops — [account child-FK SET NULL drift](sales-account-fk-set-null.md): child FKs need ON DELETE SET NULL + confdeltype self-heal; forward FK migration must ADD COLUMN IF NOT EXISTS · [import skips blanks](sales-import-refresh-skip-blanks.md): matched-contact import updates in place; never null CSV-omitted columns.
- [Hermetic ephemeral-PG tests](hermetic-ephemeral-pg-tests.md) — DB/FK tests must spin up local PG + set env BEFORE @workspace/db import; build schema via drizzle-kit push (migrations aren't blank-DB replayable); pg_ctl needs -l; bg vitest log looks hung but isn't.
- Branded email subdomains — [tiers](branded-email-subdomain-tiers.md): Tier2 branded={slug}.lpstudio.ai = WE publish Resend DNS in OUR CF zone, Tier3 custom = tenant zone; resolver custom>branded>shared; Tier2 detail in branded-email-subdomain-tier2.md · [lifecycle](branded-subdomain-lifecycle.md): ONE shared deprovision for wizard+sweep; poller advisory-lock=taskNum; retire fails-closed.
- [sales_hotlinks ON CONFLICT index missing](sales-hotlink-onconflict-index.md) — ensureHotlinkForContact's ON CONFLICT (contact_id,page_id) throws 42P10 on shared Neon (partial unique index absent); microsite_url silently blank.
- [Link-export destination registry](link-export-destination-registry.md) — no-email "generate links only" export: new destination = one registration in exportDestinations.ts; route + wizard discover from registry; rows built once in linkExport.ts.
- [Brand logo media tagging](brand-logo-media-tagging.md) — PUT /lp/brand must merge a "logo" tag onto matching light/dark lp_media rows (preserving other tags); must be tenant-scoped and idempotent; external URLs are ignored.
- Strict Facts — [no scrub](strict-facts-no-scrub.md): scan is read-only telemetry, never re-add sanitizeBlocksStrict · [review flow](strict-facts-review-flow.md): only page-backed surfaces get fact-flag rows + publish gate · [quote trust](strict-facts-quote-trust.md): quotes escape via proof point OR per-page trusted_fact_forms · [wording scope](strict-facts-wording-scope.md): opt-in (read `=== true`), restricts ONLY figures+attributed proof; 3 mirrors sync; preserve the UI literals.
- [Reference-image supply caps](reference-image-supply-caps.md) — page-create photo supply is bounded at TWO stages (per-page DOM `MAX_CONTENT_IMAGES` + mirror `MAX_REFERENCE_PHOTOS`); raise both together; safe for Brand Import (caps at 6 first-N); dedup folds variants independently.
- [URL-sourced facts trust](url-sourced-facts-trust.md) — a successfully-scraped PER-REQUEST source URL makes that generation's facts trusted (relax strict guards in both paths); inspirationUrls don't; +fillDsoCaseStudyNeutralDefaults stops DCA component-default leak.
- Section-bg rhythm (avoid all-white stacks) — [microsite](microsite-section-bg-rhythm.md): seeds self-section blocks in mergeWithDefaults · [LP generate-page](lp-section-bg-rhythm.md): seeds at generation only, skips heroes/dso/chrome/full-page; lazy-ref SELF_CONTAINED_FULL_PAGE_TYPES or use-before-init crash.
- Media read-ACL parity (lib/libraryScope.ts) — [generator catalog](generator-catalog-drawer-acl-parity.md): fetchMediaCatalog mirrors the drawer scope or generation starves; scorer ranks only TAGGED images · [storage serve ACL](storage-serve-acl-sibling-parity.md): serve route honors resolveOwnedTenantIds (reciprocal siblings) or library images 403; keep anonymous path open.
- [Shared Dialog clipping + overflow merge](dialog-grid-clip-and-overflow-merge.md) — centered grid DialogContent clips left edge when a wide child (e.g. <select> sized to longest option) blows past max-w; fix at base; overflow-hidden drops base overflow-y-auto via twMerge.
- Image fill ordering/tiering — [scraped relevance gate](scraped-image-relevance-gate.md): strict scraped need score>0 (curated >=0) · [no relaxed-curated pre-AI](ai-page-offtopic-curated-preempts-aigen.md): off-topic images fill ONLY in the last-resort pass after AI gen · [pool ordering](image-fill-pool-ordering.md): `currentReference` flag (fresh+current-host scrapes) is the strict-pass key, NOT contentScore>0; tiers beat pool order · [own-scrape starvation](image-fill-scraped-tier-starvation.md): promote own page-reference scrapes via `tags-'scraped'`; EXCLUDE_TAGS includes `logo`.
- Hero color/contrast — [tone via resolveSectionSurface](hero-tone-resolvesectionsurface.md): light/dark text from resolveSectionSurface(brand).isDark; bg-image heroes count as dark · [brand-var discipline](generic-hero-brand-vars.md): brand colors via var()+fallback; real leaks = tailwind brand utils + brand-tinted glows · [event-page dark-scrim](event-page-hero-dark-scrim.md): hero inks contrast a dark scrim base, never the flat theme palette.
- [Case-study blocks real content + approved-only](dso-success-stories-approved-only.md) — all 3 case-study blocks populate from approved Content Library rows with REAL quote/author/stat/image, ranked by locations→segment; empty library keeps built-in examples; approved stats must enter buildApprovedStatSet or get scrubbed to "X".
- Logo/reserved-media AI exclusion — [logo preservation](logo-preserve-replace-imagery.md): logos filtered out of slot collection + sanitize guard; conservative detection · [reserved tags](reserved-media-tags-ai-exclusion.md): logo/team-photo in EXCLUDE_TAGS + marker-gated backfill; team block renders via reconcileTeamMemberPhotos.
- Hand-mirrored block schemas drift (no shared import) — [template-page seeds](template-page-seed-mirroring.md): api-server seeds mirror lp-studio defaults; edit BOTH + bump SEED_MARKER · [microsite field parity](microsite-block-schema-field-parity.md): AI schema string AND normalizer must use the renderer's EXACT prop names.
- Brand-import extraction — [named token beats wash](brand-import-named-token-over-wash.md): named --brand/--primary CSS token beats pixel-sampled wash for primary; name-matcher matches role keyword as dash-segment ANYWHERE · [font matching](brand-import-font-matching.md): script-accent fonts can win heading via weight>=600, and Typekit hyphen-slugs get no Google fallback → Arial; fix in assignRoles + matchFont; renders via stored displayFontUrl.
- [New block-type graduation callsites](block-type-graduation-callsites.md) — ~9 synced callsites to graduate a mockup into a block type; block-variant.ts needs BOTH the import AND the union member, registry ENTRY is separate from createBlock case.
- [DSO block cross-tenant exposure](dso-block-cross-tenant-exposure.md) — exposing a vertical (DSO) block to ALL tenants' LP+microsite gen needs de-biasing its image-fill context (drop hardcoded "dental clinical") + a microsite member-photo guard (no reconcile pass there).
- [mockup-section optional CTA + brand-swap](mockup-section-optional-cta-convention.md) — section mockups: gate CTA on a prop (showCta bool or nullable cta), never `const showCta=true`; theme via token inline-styles, never hardcoded Tailwind color classes/avatar data.
- [dso-case-study field enumeration sync](dso-case-study-field-enumeration.md) — new dso-case-study field needs renderer + PropertyPanel + fillDsoCaseStudyNeutralDefaults + enforceApprovedCaseStudies(strict, both branches); stat-scan & copy-rewrite already recurse.
- Builder canvas — [navbar containment](builder-navbar-containment.md): keep sticky/fixed navbars in-canvas via per-block isBuilder→relative AND `[data-lp-builder]{contain:layout;isolation:isolate}` · [container queries](builder-canvas-container-queries.md): viewport-bp grids look cramped in the narrower canvas DOM; use Tailwind v4 `@container` so columns track real canvas width.
- Campaign/signal attribution — [dual-write](signal-attribution-dual-write.md): opens/clicks dual-write signals + email-send rows; tenant-scoped resolveContactByEmail · [recording points](campaign-engagement-signal-recording-points.md): record at hotlink RESOLVE; send-stamp dedup = atomic claim, never read-then-insert · [matcher](signal-attribution-matcher.md): one resolveSignalLinkage, fail-closed, exact-only; LinkedIn canonicalizer must KEEP path.
- Surface paint-vs-contrast divergence — [footer/hero](footer-paint-vs-contrast-divergence.md): paint the SAME resolved hex the contrast math uses; derive ink via resolveSectionInk · [marketing global heading trap](marketing-global-heading-color-trap.md): global h1–h6 color rule beats parent div color; headings on dark bgs need their OWN inline color · [inner-theme follows surface](insights-dashboard-theme-follows-surface.md): derive inner theme from resolved section surface (isDark), fix EVERY consumer.
- BrandLogo tone/contrast — [render convention](block-logo-brandlogo-convention.md): BrandLogo + brandHasLogo gate; wordmarks only when no logo exists · [dark-surface whitening](brand-logo-dark-surface-whitening.md): mix-blend-difference navs pass tone="onDark" · [light-surface contrast](brand-logo-light-surface-contrast.md): opt-in autoContrast; footers drive tone from actual bg darkness.
- [Marketing SFDC shared OAuth](marketing-sfdc-shared-oauth.md) — LP Settings SF integration shares sales-console sfdc_connections OAuth; form leads sync OAuth-only (client_credentials retired); callback dual-modes on signed returnTo; per-form mappings spread last into createLead.
- [no-dandy-leak-tenant live catalog](no-dandy-leak-tenant-live-catalog.md) — *-tenant variant builds from shared-DB /api/block-catalog (seed never prunes); leaks from excluded dandy-*/dso-* rows are pre-existing cleanup debt, not your regression. Don't prune shared prod.
- [Templates list blocks projection](templates-list-blocks-projection.md) — template LIST routes must project + derive count/types in SQL (jsonb_array_length + jsonb_agg WITH ORDINALITY), never ship full blocks JSONB; helpers read only block.type so type-only is safe; pages.ts GET /lp/templates still un-projected (needs consumer audit).
- Block icon fields — [dual-field](lp-block-icon-dual-field.md): one string = Lucide name OR image URL; render via IconOrImage, edit via IconPicker · [IconPicker scope](icon-field-scope.md): free-string icon fields only; constrained-union/curated-set fields stay specialized; normalizeIconValue for legacy lowercase rows.
- [New hero block gen wiring](hero-block-generation-wiring.md) — checklist: 3 image-fill callsites + 4 prompt lists + nav-dedup SELF_NAV decision; each enumerates heroes explicitly, no shared registry.
- generate-page block selection — [role brand-match](lp-generate-page-hero-selection.md): brand-MATCH all roles (not random); role ORDER from superadmin block_catalog.sort_order · [ai_enabled filter line-level](ai-block-catalog-filtering-line-level.md): prompt filter MUST be line-by-line; DSO paths filter at the generate-page callsite wrap.
- [Section bg presets](section-surface-bg-presets.md) — give a section block the gradient/brand Background list via 3 edits (interface backgroundStyle?, panel SectionBackgroundControl, renderer resolveSectionSurface + `background` shorthand + surface.base/color/isDark); unset = legacy bgColor, no regression.
- Graduated pillar/feature section blocks — [radius knob scope](section-radius-knob-scope.md): sectionRadiusClass scoped to the 9 blocks; retarget the shared fallback to change the default everywhere · [card-surface contrast](card-surface-contrast-on-brand-section.md): always-carded blocks resolve a CONCRETE card surface, never useSectionTheme.cardBg (invisible film on dark/brand).
- Section-block premium polish — [pillar/feature](section-block-premium-polish.md) / [case-study/quote](premium-section-block-polish.md): gate ALL internal polish on `isBuilder=!!onFieldChange`; AccentGlow (surface-aware) not GlowOrbs; NO_REVEAL blocks wrap header only; Grid+Bento decor-only.
- generate-page image-slot callsites stay synced — [array-prop branches](fillemptyimages-explicit-branches.md): new image-bearing prop = collectImageSlots + fillEmptyImages + sanitizeAIImageUrls, all three · [decorative-panel→real images](how-it-works-alternating-real-images.md): also +aiFillEmptyImages (most missed) · [dso- scalar imageUrl branch](dso-scalar-imageurl-fill-branch.md): dedicated dso- branch already library-fills single-image dso blocks.
- Bulk/batch AI ops — [bulk-classify](library-bulk-ai-classify.md): tenant-scoped resumable ≤20 batches; report 429+backoff, not fire-and-forget · [AI-proxy batch cap](batch-ai-proxy-reclassify.md): stalls ~20 without retry-on-429; surface every outcome · [paginated multiselect](paginated-multiselect-bulk-action.md): use the full selected id set server-side, never the loaded page.
- [Block-settings capability gating](block-settings-capability-gating.md) — chrome blocks (nav/header/footer) must be ANCHOR_ONLY in OVERRIDES; BlockRenderer now strips saved settings a block's caps don't support BEFORE wrapWithSettings (caps alone only hide controls, not applied values).
- [AI sales briefing seller direction](ai-sales-briefing-seller-direction.md) — AI account/contact briefings must explicitly frame tenant=SELLER vs account=PROSPECT (via getSalesBrandContext + separate blocks) or the model flips to "account sells to itself".
- [Custom HTML block brand font](custom-html-iframe-brand-font.md) — self-hosted brand font (Bagoss) only renders in the sandboxed Custom HTML iframe if embedded as a base64 data-URI @font-face; app's hashed /assets path + relative URLs don't resolve.
- [Product-detail fill subject-only](product-detail-fill-subject-only.md) — product-grid/dso-products-grid fills must `pick(...,biasPage=false)`; page-vocab bias + the +8 purpose boost lets a crown/logo win a denture card. Hero/feature keep bias.
- Block/brand defaults stay neutral — [baked accent colors](block-default-baked-accent-colors.md): never hardcode accent/gradient literals in createBlock defaults; dark-surface fallback = brand.accentColor, NOT primaryColor · [font @theme default](brand-font-theme-default.md): `--brand-font-*` defaults stay `var(--app-font-*)` or a face bleeds onto every fontless tenant.
- [Shared-prod test tenant cleanup](shared-prod-test-tenant-cleanup.md) — integration tests seeding tenants in the shared prod DB must tear down via catalog-discovery cascade (tenantCleanup.ts), not hardcoded child tables; stale-purge guards = slug prefix + exact name + age floor; never swallow FK errors; hooks need 120s timeout.
- [LP draft preview without a session](lp-draft-preview-token.md) — mint an lp_page_reviews token row → /api/lp/preview/:slug?reviewToken=…(&prerender=1 for HTML, &preview=app for SPA); localhost token preview has no brand vars so bg tokens fall back to blue — NOT a regression.
- Footer brand fallback — [Dandy palette leak](footer-dandy-palette-leak.md): de-brand AI palette literals to "" in BOTH microsite normalize + generate-page deBrandFooterColors · [social links](footer-social-brand-fallback.md): render falls back to brand.socialUrls; gate `showSocialLinks!==false && hasUrl`.
- [Scraped-image source-page hero rule](scraped-image-source-page-hero-rule.md) — a scrape is hero-eligible ONLY if it was the source-page hero (candidates[0]→lp-hero); enforce at 3 layers: tag, stale-dedup data, AND selection (findBestImage + validateAndDedupeAIImages hard-gate; soft penalty isn't enough).
- Segment block vocab/pool — [approval expansion](segment-approval-vocab-expansion.md): approved_segments UNIONS (never clamps) AI vocab, fail-closed · [pool containment](segment-pool-containment-callsites.md): pool-only constraint at 3 post-parse points · [explicit DSO block requests](explicit-block-requests-dso.md): prompt override + deterministic post-normalize inject · [exemplar structure-anchoring](microsite-exemplar-structure-anchoring.md): exemplars quality-only + path-dependent temperature.
- [Media library search ignores tags](media-library-search-tags.md) — `/lp/media/images` free-text `q` matched title only; hash-titled tagged assets were invisible; must OR title ILIKE with a tags-array ILIKE.
- generate-page image pipeline — [scrape gating](generate-page-scrape-gating.md): scrape/mirror ONLY modal perRequestUrls, NEVER brand inspirationUrls/homepage · [collectImageSlots dual role](collectimageslots-restore-vs-fill.md): manual-only image fields gated `if(includeEmpty)`, never deleted · [product library consumer](product-library-blocks-consumer.md): enforceProductLibraryBlocks AFTER image-fill, 1:1 prop names · [harvest grace window](reference-image-harvest-grace.md): consumer patience >= producer's awaited tag bound; never decouple tagging (Autoscale freezes post-response).
- [Firecrawl not a single choke point](firecrawl-not-single-choke-point.md) — scrapes flow through 3 INDEPENDENT clients (routes/lp/firecrawl.ts, lib/brand-import/evidence.ts, routes/sales/draft-email.ts); any Firecrawl-wide policy (cost cap, retry, SSRF) must cover all 3 or silently misses two. Cap+retry cover page/microsite scrapes only.
- lp-studio dev client crashes — [HMR intermediate-edit crash](hmr-intermediate-edit-crash.md): "just-edited feature missing" = mid-edit HMR crash into BlockErrorBoundary; compare console timestamps vs `[vite] hot updated` first · [Vite dep-cache hook crash](lp-studio-vite-cache-hook-crash.md): app shell spins w/ "Invalid hook call" = stale dep cache serving a 2nd React copy; rm node_modules/.vite (artifact+root) + restart.
- AI copy refinement passes — [critique timeout fail-open](critique-pass-timeout-failopen.md): corrective rewrite behind shared semaphore timed out at 3s and failed open (now 12s); rewrites ~2 blocks only, never churn buildBrandContext · [rewrite_copy instruction threading](copilot-rewrite-instruction-threading.md): "Ask AI" `instruction` must flow through all 3 layers (BuilderEditor→refreshBlockCopy→/lp/copy-generate) + sys/user prompt or the refinement is silently dropped.
- [Nav-dedup both paths](nav-dedup-both-paths.md) — stripRedundantLeadingNav (drop standalone nav before a self-nav hero) must run on BOTH template + freeform generate-page paths; template path skipped it → [nav-header, hero] shipped two navbars; code fix only affects NEW pages.
- Hardcoded media gating/removal — [preloaded slug-gating](preloaded-media-slug-gating.md): gate Dandy-only media on isProtectedEnterpriseSlug (dandy+dandy-smb), not a single slug · [remove image everywhere](blocked-image-removal-surfaces.md): an unwanted photo id hides in SIX places; grep the bare id repo-wide (excl dist)=0 before done.
- Outline-driven generation — [LP authoritative](lp-generate-page-outline-authoritative.md): outline PLACED via resolve+reconcile; outlineActive gates OFF role enforcement but KEEPS chrome; props-only · [microsite hard-override](microsite-outline-hard-override.md): outline gates OFF every add/remove/reorder pass; styling-only passes exempt · [brand-default collapse](brand-default-outline-neutral-role-defaults.md): NEUTRAL_ROLE_DEFAULT_BLOCKS as roleDefaults at BOTH generators or generic tenants collapse to hero/cta/footer.
- [Create-page Template vs AI paths](create-page-template-vs-ai-paths.md) — "AI didnt customize my template" = clone-path confusion: Template tab copies VERBATIM, only AI Generate rewrites copy; generate-page path is sound (reproduced on TRIOS5 id 272).
- [Generation stage rail wiring](generation-stage-id-dual-union.md) — GenerationStageId=closed union dup'd backend+frontend; data-driven rail via caller stageDefs; a per-path step goes in THAT caller's stageDefs ONLY (not shared DEFAULT_STAGE_DEFS) + tick start→done every path; open SSE emitter BEFORE the slow work.
- [Marketing multi-page served from R2 edge](marketing-multipage-served-from-r2-edge.md) — Replit static origin SPA-rewrites /*→root index.html; per-route marketing HTML MUST be served by CF worker Tier 0.5 from R2 `_studio-marketing/*`; 2-step rollout (publish then wrangler deploy).
- [Merge-field single source](merge-fields-single-source.md) — sales/landing merge fields live ONLY in lib/notification-variables (SALES_CONTACT/LANDING_PAGE); inserters source from it; tokens must be the send-path snake_case forms; landing-page vars are PAGE-only (don't resolve in email body).
- Tenant host rules — [managed subdomain slug-shadow](managed-lp-subdomain-slug-shadow.md): free managed `<label>.lpstudio.ai` must NOT equal another tenant's slug (exact host beats wildcard); guard at signup+POST+DELETE+backfill; only CUSTOM domain is plan-gated · [canonical tenant host](invite-email-canonical-tenant-host.md): emit workspace URLs via shared canonicalTenantHost/SignInUrl; never `domain?…:APP_URL` nor req.get('host') (prod proxy rewrites it).
- [Draft Email research vertical-hardcoding](draft-email-research-vertical-hardcoding.md) — Sales "AI Draft Email finds no research" = Perplexity prompts dental-hardcoded; derive industryHint from account, empty=search whole web; not a key/timeout issue.
- [jsdom strips bg-image url()](jsdom-bg-image-url-stripped.md) — block render tests (jsdom) lose `background-image:url(<unquoted>)` from markup; assert the conditional overlay div's inline opacity, not the image URL.
- Unique-violation handling — [error code on cause](drizzle-error-code-on-cause.md): SQLSTATE is on `err.cause.code`, use lib/dbErrors `isUniqueViolation` (walks cause chain) · [lp_pages slug auto-dedupe](lp-pages-slug-dedup.md): dedupe per-tenant (foo-2/-3, keep ≤255) like the clone path + isUniqueViolation catch.
- [Microsite segment optional → core](microsite-segment-optional-core.md) — empty segment = synthetic core (buildSegmentSection ""); unknown id still 400; MUST also pass accountSegment null or buildSystemPrompt's matchedSegment re-leaks the account's own audience directive.
- [Builder block-palette tab routing](builder-block-palette-tab-routing.md) — block→tab is by category vs 4 hardcoded lists in BuilderEditor.tsx; move a category by editing all in lockstep or it shows in both tabs or neither.
- [Microsite nav/hero vs recipe scope](microsite-navhero-vs-recipe-scope.md) — post-gen nav/hero enforcement spans all 3 microsite passes; recipe VARIETY is neutral-freeform-only; freeform vocab = general LP set ∪ extras (micrositeFreeformVocab); nav-DEDUP and hero-UPGRADE type-sets are decoupled.
- [OG picker drawer+capture](og-picker-drawer-and-capture.md) — /lp/in-use-images must tenant-scope+cap (full-res pile garbles mobile-Safari decode); html-to-image capture needs skipFonts (freeze) + storage ACAO header (cross-origin imgs dropped) + cacheBust:false.
- [Block brand injection after governance](block-brand-injection-after-governance.md) — per-block accent AND imagery reapply (event-page theme.primary + reapplyEventPageImagery) must run AFTER enforceAiModes in BOTH generate-page paths or governance reverts to catalog defaults; copy reverts only GOVERNANCE_IMAGE_FIELD_KEYS (src yes, heroImageUrl no), locked resets all.
- [Heartland hero companyName = target-co slot](heartland-hero-companyname-semantics.md) — companyName is the PROSPECT name, NOT seller brand; blank on LPs, fill from account on microsites; FINAL guard after enforceAiModes; copy-generate is not a fill path.
- [Hero secondary-CTA url routing](hero-secondary-cta-url-routing.md) — url-mode secondary CTA on a published hero navigates to the PRIMARY url (shared onCtaClick→resolveCtaUrl(primary)); pre-existing parity across editorial/ai-scan heroes, modal actions unaffected; real fix is platform-wide.
