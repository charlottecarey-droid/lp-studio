- [Dev env source](dev-env-source.md) — dev workflows read env from `.replit` `[userenv]` + gitignored `.env`; Replit Secrets are agent/deploy-only, NOT injected into workflow shells.
- [pnpm overrides location + range](pnpm-overrides-location-and-range.md) — overrides live ONLY in pnpm-workspace.yaml; never add a package.json pnpm.overrides block (silently clobbers every workspace pin/prune); bump a pinned dep at its pin; `^` not `>=` for toolchain-deep deps.
- [Prod ships dev .env + clobbers Secrets](prod-env-dev-dotenv-clobber.md) — Autoscale ships gitignored dev `.env`; dotenv `override:true` lets its BLANK secrets overwrite injected Replit Secrets → empty-secret boot-guard crash-loop. Gate dev `.env` load on NODE_ENV!=="production". `viewEnvVars` true=key exists, not non-empty.
- [e2e runButton restart race](e2e-runbutton-restart-race.md) — preview blanks every restart b/c runButton ran e2e, whose webServer rebuilds the SHARED api-server dist → crash; fix=removeWorkflow(e2e) (autoStart:false/clearValidationCommand don't work).
- [Deployment image 8 GiB trim](deployment-image-size-trim.md) — publish dies at packaging "image over 8 GiB"; .replit not editable, so append `rm -rf` (.pnpm-store/.config-chromium/attached_assets) to artifact production.build; keep .cache (runtime playwright) + node_modules.
- [Git merge conflicts as main agent](git-merge-conflicts-main-agent.md) — agent can't write under `.git/` (no add/commit/rm/reset/fetch); resolve working tree (strip markers, pick ONE side per interdependent rewrite) + validate; user runs git + clears stale `.git/*.lock` via Shell.
- AI image/icon sanitize — [trust-bar numeric-only](ai-trust-bar-numeric-only.md): stats=value+label only, skip STAT_BAR_BLOCK_TYPES at every assign/restore in both gens · [icon URL hard-strip](ai-icon-url-hard-strip.md): stripUrlValuedIcons blanks URL-shaped icons across ALL keys (spotlightIcon/iconName too) · [icon-only item photos](icon-only-item-photos-sanitize-strip.md): strip items[].image in sanitizeAIImageUrls when useItemPhotos!==true · [customer-logo slots](customer-logo-slot-no-fill.md): EXCLUDE logo boxes from collectImageSlots+fillEmptyImages (NOT sanitize force-clear), renderer gates box on hasImage||builder, guard by blockType.
- [Modal theme fallback callsites](modal-theme-fallback.md) — BrandConfig modal fallback must happen at every EmailCaptureModal callsite; the modal itself has no brand context.
- [EmailWYSIWYGEditor mergeVars contract](email-wysiwyg-mergevars-bare.md) — editor mergeVars/insertMergeVar need BARE names (it wraps {{…}} itself); wrapped/empty values cause the empty-{{}} bug; plain-text chips keep wrapped tokens.
- [Workspace login branding](workspace-login-branding.md) — tenant logins must read as LP Studio (logo+wordmark), Dandy is the sole white-label exception; no custom tenant logo, no "powered by".
- [LP Studio dev marketing host](lp-studio-dev-marketing-host.md) — in dev, '/' renders the marketing site; E2E tests needing the SaaS shell must use '/?preview=app'.
- [LP_PUBLIC auth hydration](lp-public-auth-hydration.md) — auth-conditional logic on LP_PUBLIC routes needs `optionalAuth` middleware or `req.authUser` is always undefined.
- [Tenant shell vs marketing-flash band-aids](tenant-shell-vs-marketing-flash.md) — fixing tenant-host flash requires serving a different shell from the CF worker; inline "hide #root" scripts in index.html are NOT a real fix.
- Stripe infra — [apiVersion pinning](stripe-api-version-pinning.md): copy the literal from stripe/esm/apiVersion.d.ts verbatim after each SDK bump · [dunning order](stripe-dunning-email-ordering.md): in payment_failed downgrade FIRST then send best-effort/bounded/de-duped, never await a hangable send before revoking · [accounts self-heal gating](stripe-accounts-selfheal-gating.md): GATE stripe.accounts boot heal on tracker 0050 or it wedges the package's non-idempotent 0046+ migrations.
- [CF worker routes source of truth](cf-worker-routes-source-of-truth.md) — never put `routes = [...]` in tenant-host-router/wrangler.toml; wrangler reconciles and deletes API-provisioned tenant routes. Use sync-worker-routes.ts.
- [Dandy microsite CF cutover](dandy-microsite-cf-cutover.md) — cut partners/lp.meetdandy.com to the edge via CNAME→lpstudio.ai; CNAME rule forces removing the per-host replit-verify TXT (token persists on apex). Verify via DoH + --resolve.
- [AI copy voice always-inject](ai-copy-voice-always-inject.md) — brand voice + active brief must inject on every LP Studio AI copy call via the shared brand-and-brief builder; never gate them on a colors/fonts toggle.
- [AI segment-hierarchy = priority not override](ai-segment-hierarchy-priority-not-override.md) — segment-vs-core directive must say "segment LEADS/takes priority, still draw on brand core" NOT "OVERRIDES/NON-NEGOTIABLE"; absolute override starves brand depth → thin copy; 4 mirrored spots; avoidPhrases/persona are the real anti-leak controls.
- [AI reasoning model empty output](ai-reasoning-model-empty-output.md) — gpt-5-* reasoning models under a tight max_completion_tokens return EMPTY content (silent); standard is gpt-4o (copy/SEO) + gpt-4o-mini (cheap JSON/vision extractors), NEVER gpt-5-mini — a bulk "upgrade" to it silently broke brand-import + image-tagging.
- [AI copy brand-voice system anchor](ai-copy-brand-voice-system-anchor.md) — generic/identical-across-tenants full-page copy = brand voice only in USER prompt; fix = anchor it in the SYSTEM prompt too (additive), never churn buildBrandContext or widen critique.
- [Legacy default-on boolean config](legacy-default-on-boolean-config.md) — new default-ON BrandConfig flags must be read as `!== false` at every callsite; truthy checks silently flip legacy rows to OFF.
- [Drizzle journal must list every migration](auth-exchange-codes-schema.md) — adding a .sql to lib/db/migrations does nothing unless its tag is also in meta/_journal.json; missing entry = silent skip on every release; symptom is prod 42703 "column does not exist" on the cross-domain OAuth callback.
- [Fetch redirect SSRF](fetch-redirect-ssrf.md) — backend fetches of untrusted URLs must use `redirect:"manual"` and re-validate each hop's host; `"follow"` defeats the initial public-IP check.
- [Tenant-scoped queries fail closed](tenant-scoped-query-fail-closed.md) — DB helpers reading tenant rows must take required tenantId, or return empty on null; never fall back to unfiltered or `limit(1)` queries.
- Page head meta/title — [OG/SEO pipeline](og-meta-pipeline.md): scrapers read pre-baked HTML (marketing prerender bakes usePageMeta; injectPageMeta for tenants); a managed tag = 3 coordinated edits (don't forget the injection upsert) · [app-shell tab title](app-shell-tab-title.md): authed tab="LP Studio - {tenant} - {page}" via AppLayout/resolveAppPageName, strips og:image/desc; AppLayout is each page's CHILD so the page's own title effect wins.
- [Marketing-site superadmin config baking](marketing-site-config-baking.md) — to make a marketing value editable AND reach scrapers: DB+public endpoint, home.tsx global+fetch w/ field-fallback, prerender best-effort `pg` read (NOT @workspace/db) + addInitScript global.
- [Stripe Checkout E2E test clock](stripe-checkout-e2e-testclock.md) — can't complete hosted Checkout headlessly; create the sub on a test-clock customer + drive the signed sub event through stripeWebhookHandler; gate on STRIPE_SECRET_KEY (pool is PROD).
- Plan/trial state — [tier model](stripe-tier-model.md): cancel→free, Checkout only if no active sub else Portal, untrusted price fails closed, price change=archive+recreate · [live accessor](plan-config-live-accessor.md): read getPlanFeatures+getTenantPlan not static PLAN_FEATURES · [no plan='trial' write](trial-plan-write-paths.md): date-window only, read-normalizes to growth, guardrail test · [free closes trial](plan-free-write-trial-close.md): splice CLOSE_TRIAL_ON_FREE_SQL or effectivePlan re-lifts the trial.
- [LP hero overlayOpacity scale](lp-hero-overlay-opacity-scale.md) — LP hero overlayOpacity is a 0-100 percent (renderer ÷100); AI prompt schemas must use whole-number percents, not 0-1.
- [Composite lib dist drives types](lib-db-composite-dist-types.md) — editing a composite lib schema → consumer reads stale dist → phantom "property does not exist"; fix with `tsc -b <lib> --force` (incremental no-ops; plan-config needs db+plan-config; a sibling *.test.ts can block emit). See one-pager-types-composite-dist.md, plan-config-composite-dist.md, composite-lib-test-exclude.md.
- [Generated block contrast](generated-block-contrast.md) — AI page blocks must derive text/badge/button colors from their actual bg, never pair two brand colors; fix at render-block layer · [preset-style CSS-var bypass](lp-preset-style-bypass-contrast-guards.md): a block `style` preset sets bg to a var() string → isValidHex false → WCAG guards silently skip; resolve preset→hex BEFORE the contrast math · [block brand-var surface bypass](block-brand-var-surface-contrast.md): a block whose DEFAULT bg is `var(--brand-primary)` feeds that var into resolveSectionInk → treated as white → black headline on dark brand; resolve var→live brand hex (useBrandConfig) before contrast math · [contrastTextColor mispick](contrast-text-color-mispick.md): non-gamma luminance + 0.55 cutoff mispicks white on mid-tone fills; CTA labels use pickContrastingColor(…,4.5).
- [Prod DB is Neon not Helium](prod-db-is-neon-not-helium.md) — app uses NEON_DATABASE_URL; executeSql dev/prod targets read stale Helium DB. Verify prod by querying Neon directly; dev-shell scripts via lib/db hit PROD.
- [R2 boot sweeps vs startup probe](r2-boot-sweeps-vs-startup-probe.md) — build-OK-but-promote-failed w/ zero logs = startup-probe starvation; keep R2 sweeps off app.listen path; all S3 clients via buildR2S3Client (200 sockets).
- [Content Series guest fields](content-series-guest-fields.md) — podcast RSS has no guest field (author=producer); populate via AI prose extraction; hero must source guest from the targeted episode alone.
- [Block role tags](block-role-tags.md) — semantic tag vocab+defaults live ONLY in lp-template-engine/block-tags.ts; DB overrides via resolveBlockTags; AI guide parses allowed blocks from system-prompt `- "type":` bullets (silently empties if format changes).
- One-pager generators (sales) — [brand asset URLs](one-pager-brand-asset-urls.md): Dandy images=static public files in salesConsole.onePager*, empty=neutral, root-relative need base-path norm · [LP-viewer framing](lp-viewer-onepager-framing.md): share the landing-page-viewer pipeline, detect via `one-pager-hero` block, keep builder+linkedPage in sync · [heading font](one-pager-heading-font-resolution.md): detect brand heading via getFontList()["Bagoss"], never ensureBagoss in non-Agreement gens · [body-control parity](one-pager-body-control-parity.md): each generator reads bodyCfg independently — an editor slider no-ops unless THAT template reads the field · [partner paths](partner-onepager-pdf-only.md): Partner Practices renders BOTH jsPDF + a template-branched web one-pager, carries boldHeading.
- inject() route tests — [in-process inject](vitest-listen-hangs-inproc-inject.md): vitest pool never fires app.listen; use inject() not port+fetch · [rate-limit key](rate-limited-route-inject-tests.md): inject sets no req.ip → shared limiter key, set trust proxy 1 + unique X-Forwarded-For per call · [post-response side-effects](inject-test-post-response-sideeffects.md): inject resolves on res.end; fire-and-continue work needs a bounded poll, not immediate read.
- [E2E Dandy-gating both paths](e2e-dandy-tenant-limit.md) — client gate=brandName (royal fixture w/ brandName:"Dandy"), server gate=slug (impersonate seeded dandy-smb + cleanup rows); both paths testable.
- [Chili Piper booking tracking](chili-piper-booking-tracking.md) — 2 surfaces (hero ChiliPiperButton + cta-button→ChiliPiperModal hook); never gate booking recording on lead PII; only `if(!url)return`; omit testId/variantId unless a real A/B test.
- [mockup-sandbox generated file untracked](mockup-sandbox-generated-file.md) — src/.generated/mockup-components.ts is gitignored (regenerated on dev start); never re-add, it conflicts every rebase.
- [Pooler advisory-lock leak](pooler-advisory-lock-leak.md) — session pg_try_advisory_lock leaks on Neon -pooler; customDomainPoller integration tests flake (0 sends); clear orphan via pg_terminate_backend; prefer pg_advisory_xact_lock.
- [Email auth token host-binding](auth-email-token-host-binding.md) — every email-token redeem route (verify/magic-link/reset) must re-check `redeemed.targetHost` vs current host; redeemEmailToken enforces single-use/purpose/TTL only.
- [PDF brand-font embedding](pdf-brand-font-embedding.md) — jsPDF addFont swallows bad bytes (crashes next getTextWidth) so magic-byte-validate first; Google v1 /css + curl UA returns full-charset TTF (no unicode-range subsetting).
- Platform email — [hard-fallback](system-email-hard-fallback.md): transactional/auth senders need a self-contained HTML fallback, never wrap it in the editable shell (blank shell breaks auth) · [token coordinated edits](email-token-coordinated-edits.md): new token = 4 synced edits (var catalog+sender+previewData+HTML) or it leaks {{token}}/renders blank (derived excepted) · [CTA always baked](lifecycle-email-cta-always-baked.md): authored body_html always emits the CTA, byte-identity only holds when ctaUrl present.
- [Running long e2e via workflow](running-long-e2e-via-workflow.md) — bash 120s cap + reaped detached procs mean long Playwright specs must run in a temporary console workflow; poll getWorkflowStatus.
- [notification_sends missing on drifted DB](notification-migration-skipped-on-drifted-db.md) — 0041 journaled below high-water mark = drizzle skips it forever; now self-healed in migrate.ts + dispatcher rethrows 42P01/42703 loudly; prod (Neon) verified OK.
- [Tenant LP default-noindex](tenant-lp-default-noindex.md) — all tenant LPs noindex by default (Dandy slug-excepted), fail CLOSED; emit via robots <meta> + X-Robots-Tag in BOTH R2 prerender (x-robots customMetadata, both CF workers) AND live paths.
- [Cross-instance notification broker](notification-cross-instance-broker.md) — SSE pushes fan out via LISTEN/NOTIFY; NOTIFY ok on pooler, LISTEN needs non-pooled (strip `-pooler`); INSTANCE_ID skips own echo.
- [drizzle multi-statement = extended protocol fail](drizzle-multistatement-extended-protocol.md) — run a whole .sql file via raw `pool.query(string)` (simple protocol); `db.execute(sql.raw(...))` sends params → extended → "cannot insert multiple commands".
- [jsPDF fill-color encoding](jspdf-fill-color-encoding.md) — PDF byte color guards: fills=2-decimal `rg`, text=3-decimal; hardcode brand RGB literals, not DANDY_PALETTE-derived.
- [Workflow engine hard-fallback](workflow-engine-hard-fallback.md) — a MATCHED-but-non-executable email workflow must still run the code-default fallback; gate on isWorkflowExecutable (template existence) at runtime + save-time.
- [Tenant email audit metadata-only](tenant-email-audit-pii.md) — tenant test-send audit must NOT store recipient address (unlike platform editor's sentTo); log a flag + actor only.
- [notification_templates channels stale row](notification-channels-stale-row.md) — channels col = capability+toggle; adding a channel in code never backfills existing rows; stale row hides editor ("handled in code"), fix via data UPDATE.
- [DB-only notification templates](db-only-notification-templates.md) — blank-slate templates have no code registry entry; every admin route must resolve via getNotificationTemplate, not NOTIFICATION_TEMPLATES[key], or it 404s.
- [E2E networkidle vs SSE](e2e-networkidle-sse.md) — SaaS-shell routes hold a persistent SSE stream; `waitUntil:"networkidle"` never settles (90s goto timeout). Use domcontentloaded + element asserts.
- Brand-import — [mirror vs vision](brand-import-mirror-decoupled-from-vision.md): reads referenceImageUrls from RAW results too (flatten drops failed dims); vision fails often → empty lp_media → blank src; lazy attrs need a usable URL · [screenshot two sizes](brand-import-screenshot-two-sizes.md): full-res 8MB evidence for vision, persisted/cached preview downsampled <5MB via buildScreenshotPreviewDataUrl · [evidence timeouts](brand-import-evidence-timeouts.md): TWO compounding caps (per-scrape firecrawl + outer buildEvidence); outer budget must DERIVE from sum of inner or slow sites import nothing · [cache poisoning](brand-import-cache-poisoning.md): never store/serve an all-failed payload or one transient fail blocks re-scrape 24h.
- [Workflow "once" date fail-closed](workflow-once-date-failclosed.md) — date-string sanitizers must round-trip components through Date.UTC; regex + `!isNaN(Date.parse())` silently accepts impossible dates (2026-02-31 normalises) → fires wrong day.
- [Retry "deduped" ≠ delivered](workflow-retry-deduped-not-delivery.md) — workflow send-failure retry must verify the occupying notification_sends row is status='sent'; a stale 'pending' claim falsely resolves an undelivered send.
- [LP full-page block wiring](lp-fullpage-block-wiring.md) — new full-page blocks must mirror content-series at EVERY callsite incl. BlockRenderer NO_REVEAL (else sticky/scroll breaks only on published page) + keyword-gated AI freeform injection.
- [Tenant email logo absolute URL](tenant-email-logo-absolute-url.md) — tenant notification email logos stored root-relative (/api/storage) must be normalized to absolute via app public host or they drop to text + render broken in mail clients.
- [Settings hub consolidation](settings-hub-consolidation.md) — Settings is ONE tabbed SettingsPage (owns AppLayout); section bodies are `*Content` exports; broadcast-recipient "default" = NO row (DELETE to restore, never save-empty).
- [Storage CORP for sandboxed previews](storage-corp-sandboxed-preview.md) — assets in sandboxed/opaque-origin iframes (email preview srcdoc) need `Cross-Origin-Resource-Policy: cross-origin` or logos silently break; test in the sandbox, not a normal load.
- [Global template seed + gating](global-template-seed-and-gating.md) — new global/flagship templates need a `global_templates_seed_vNN` marker bump in migrate.ts; enriched feeds all 3 surfaces; DB globals are NOT industry-gated server-side (only client LP_TEMPLATES are).
- [Snapshot provenance footer per-host](snapshot-provenance-footer-per-host.md) — published snapshot bakes "Sent by" footer maximal (prerender gates shared subdomain) then strips per host; keep marker+gate+prerender-host in sync.
- [Email preference center grouping](email-preference-center-grouping.md) — personal prefs grouped into fixed taxonomy; opt-out stored as ONE `grp:<id>` row (durable for future templates), checked inside isOptedOut; never per-template.
- [Sales layout-defaults save feedback](sales-layout-defaults-save-feedback.md) — "/api" is same-origin; CSRF auto-injected by a global window.fetch interceptor (curl 403 is expected). Real bug: write helper swallowed errors + no res.ok check → false "saved". Never swallow source-of-truth writes.
- [Tenant custom-html calculator edits](tenant-custom-html-calculator-edits.md) — calculator pages live in lp_pages.blocks props.html (DB, not repo); dump live block first, update Neon + republish, verify via /api/lp/page/:slug.
- [lp_leads field keys tenant-authored](lp-leads-field-keys.md) — fields jsonb has no fixed schema; reading by exact key (email/firstName) silently drops leads using "Email Address"/"First Name"; normalize keys. Empty dashboard can also just be api-server down.
- [Launch-path DB reads in try](launch-path-db-reads-in-try.md) — bare "Failed to launch campaign" (no detail) = unhandled throw before the route try → non-JSON; every send-path DB read must be INSIDE the try + withDbRetry.
- [Sales microsite account linkage & business-case templates](sales-microsite-account-linkage.md) — account→microsite resolves via hotlinks+pageVariables+account_id/sfdc_account_id cols (keep all callsites in sync); business-case templates are global-only single-block monographs (salesMode visibility + deep-merge over authored props + scoped templateId lookup).
- [Imported button CSS marker](imported-button-css-marker.md) — buttonStyleRaw applies page-wide via `lp-brand-btn` class + injected `!important` stylesheet; outline/secondary buttons reusing getButtonClasses must pass `{imported:false}`.
- [Conversion-scoring shape detection](conversion-scoring-shape-detection.md) — analyzeBlocks must detect hero/social/trust/booking/imagery by object SHAPE not exact prop names (premium/DSO blocks use bespoke names); keep narrow imageCount (speed) vs broad hasImagery (visual) split.
- [Edge WAF blocks email-HTML bodies](edge-waf-html-request-body.md) — prod-only minimal-HTML 403 (Cloudflare, no origin log) on POSTs carrying email HTML (`href="{{token}}"`); fix = base64-wrap JSON body (now GLOBAL in lp-studio api-fetch interceptor), decode in a global middleware.
- [Business-case view-time var parity](business-case-viewtime-var-parity.md) — {{company_name}}/{{practice_count}} fill at BOTH generation bake and view-time resolve; both must use shared businessCaseVars helper or literal tokens leak on /p/:token pages.
- [Resend from stale deployment](resend-from-stale-deployment.md) — platform-email 403 naming a domain ≠ current RESEND_FROM_EMAIL = stale deployment env (snapshots at boot); fix is republish, not code; Resend subdomains are distinct from apex.
- [generate-page nav/footer auto-injection](generate-page-chrome-injection.md) — AI-page gen force-injects nav/cta/footer server-side; prompt-only "don't add X" rules need a matching post-processing guard (isSingleFullPageBlock for self-contained full-page blocks).
- [Tenant-facing placeholder branding](tenant-facing-placeholder-branding.md) — shared app-UI input placeholders must be brand-neutral (Acme/example.com/yourcompany.chilipiper.com), never Dandy; scope de-brands to `placeholder=` attrs only, never value/default constants.
- [Turnstile secret-presence gating](turnstile-secret-presence-gating.md) — global secret enables Turnstile in dev+e2e too; prod boot guard fails publish at startup probe; needs paired SITE key + `TURNSTILE_SECRET_KEY:""` in playwright.config.
- [Trial phone gate](trial-phone-gate.md) — one-trial-per-SMS-phone gate is fail-safe-OFF on twilioConfigured() (dev/e2e keep legacy signup); client echo of canonical E.164 is safe (Twilio Verify gates it).
- [OAuth login-CSRF state nonce](oauth-login-state-csrf.md) — both OAuth callbacks redeem a server-stored single-use nonce (oauth_login_states) before token exchange; test the gate hermetically via the oauth_not_configured fall-through.
- [Workflow tick integration isolation](workflow-tick-integration-isolation.md) — testing runWorkflowTick on shared prod DB: far-future delay + drive only seeded rows (sweep is global → steals dispatch & can send real bucket members); seed admin bucket not superadmin (sibling asserts full-set equality).
- [Scheduled trigger timezone math](scheduled-trigger-timezone.md) — schedules carry optional IANA timezone (default UTC); dueOccurrenceId resolves all boundaries+fire instant in-zone, occurrence id = LOCAL date; invalid tz fails closed.
- [lp-studio typecheck via validation](lp-studio-typecheck-via-validation.md) — lp-studio tsc is >12min (past bash 120s cap) + detached procs get reaped; run it through the validation skill, not raw/background bash.
- [Snapshot render-version reconcile](snapshot-render-version-reconcile.md) — published R2 snapshots self-heal via manually-bumped CURRENT_RENDER_VERSION metadata + post-deploy reconcile; dev shares prod's R2 bucket, so NEVER run the reconcile/backfill from dev.
- [Sales delete allows synced rows](sales-delete-synced-rows.md) — account/contact delete endpoints dropped the isNull(salesforceId) guard; synced rows hard-deletable but may resync; UI warns via amber note.
- [Undo-delete restore](undo-delete-restore.md) — hard-delete Undo = capture-and-reinsert: DELETE returns rows under `restore`, POST /restore re-inserts via restoreRows (forces tenant/page scope, idempotent onConflictDoNothing, preserves ids+timestamps).
- [Template thumbnail capture](template-thumbnail-capture.md) — gallery cards show thum.io screenshots (thumbnailUrl→ogImage→gradient); isPlaceholderTemplateLabel duplicated in templates.ts + backfill script; backfill is a deliberate prod job.
- [Published LP URL tenant host](lp-page-url-tenant-host.md) — getLpPageUrl must build from user.tenantHost (microsite→tenantHost/lp→origin), never window.location.origin; admin host has no tenant binding → 404.
- [Conversion-score metrics](conversion-score-pagespeed.md) — Page Speed prefers real PSI via TTL cache + non-blocking bg fetch (falls back to block-count; VITEST off); Conversions=leads+non-form events, CVR over visits; details also in conversion-score-conversions-metric.md.
- [Email footer postal address](email-footer-physical-address.md) — each shell sources its OWN {{physicalAddress}}; never brand-prefix the line (stray "·" when blank); ensureFooterAddress injects at render for custom footers missing the token; full-custom welcome body must carry the token itself.
- [Tenant default email sender](tenant-default-email-sender.md) — every tenant sends zero-setup as {Brand}<slug@mail.lpstudio.ai>; all send paths resolve via resolveTenantSender + fail closed on unverified custom domains (no more "not configured" 400).
- [Sales account child-FK SET NULL drift](sales-account-fk-set-null.md) — account delete 500s on child FKs lacking ON DELETE SET NULL; needs forward migration + confdeltype self-heal; a forward FK migration must ADD COLUMN IF NOT EXISTS its referenced col (early-push tables can lack it).
- [Sales import skips blank fields](sales-import-refresh-skip-blanks.md) — matched-contact import updates in place (no dupes, FK links kept) and must NOT null columns the CSV omits; blank no longer clears (tradeoff).
- [Hermetic ephemeral-PG tests](hermetic-ephemeral-pg-tests.md) — DB/FK tests must spin up local PG + set env BEFORE @workspace/db import; build schema via drizzle-kit push (migrations aren't blank-DB replayable); pg_ctl needs -l; bg vitest log looks hung but isn't.
- [Branded email subdomain tiers](branded-email-subdomain-tiers.md) — 3 sending tiers; Tier2 branded={slug}.lpstudio.ai = WE publish Resend DNS in OUR CF zone (auto, exposed when provisioned, resolver fail-closes on live status), Tier3 custom = tenant zone (wizard); resolver custom>branded>shared; Tier2 specifics in branded-email-subdomain-tier2.md.
- [sales_hotlinks ON CONFLICT index missing](sales-hotlink-onconflict-index.md) — ensureHotlinkForContact's ON CONFLICT (contact_id,page_id) throws 42P10 on shared Neon (partial unique index absent); microsite_url silently blank.
- [Shared lead/test heuristic](shared-lead-heuristic.md) — isTestLead/leadName/fieldAccessor live ONLY in @workspace/lead-utils; all 4 lead surfaces must agree; test-filter is in-memory (JS, not SQL); gibberish detection is conservative.
- [Link-export destination registry](link-export-destination-registry.md) — no-email "generate links only" export: new destination = one registration in exportDestinations.ts; route + wizard discover from registry; rows built once in linkExport.ts.
- [Drizzle ANY(array) vs IN](drizzle-any-array-not-in.md) — bare JS array expands to `($1,$2)` tuple: OK for IN, INVALID for `=ANY()`; throws only on non-empty arrays; fix via `sql\`ARRAY[...]::text[]\``.
- [Page-path display microsite-aware](lp-path-display-microsite-aware.md) — many scattered callsites hardcode `/lp/<slug>`; microsite tenants serve at root; labels use micrositeDomain ternary, live links use getLpPageUrl, each component needs its own useAuth.
- [Content-series brand accent/logo injection](content-series-brand-accent-logo-injection.md) — AI-generated content-series blocks must inject the brand's accentColor (as theme.primary) and logoUrl via applyContentSeriesBranding; non-logo/whitespace brands are ignored to allow text-logo fallback.
- [Brand logo media tagging](brand-logo-media-tagging.md) — PUT /lp/brand must merge a "logo" tag onto matching light/dark lp_media rows (preserving other tags); must be tenant-scoped and idempotent; external URLs are ignored.
- [Branded subdomain lifecycle](branded-subdomain-lifecycle.md) — Tier 2 branded email subdomains: ONE shared deprovisionBrandedEmailSubdomain() for wizard+sweep; pollers convention advisory-lock=taskNum (415/783/787); retire fails-closed.
- [No-reveal sticky blocks](no-reveal-sticky-blocks.md) — blocks with internal `position: sticky` (vertical-tabs/roi-calculator/practice-nav) MUST be in the NO_REVEAL set; the scroll-reveal motion.div transform breaks the sticky containing block, but only on published pages.
- Strict Facts — [no scrub](strict-facts-no-scrub.md): no longer rewrites unapproved AI stats, scanForUnapprovedStats is read-only telemetry, never re-add sanitizeBlocksStrict · [review flow](strict-facts-review-flow.md): only page-backed surfaces get an lp_page_fact_flags row+publish-gate, AI emails advisory-only · [quote trust](strict-facts-quote-trust.md): quotes escape via approved/scraped proof point OR per-page trusted_fact_forms, POST validates forms vs on-page quotes, clone copies them · [wording scope](strict-facts-wording-scope.md): STRICT_FACTS_INSTRUCTION (default-ON) restricts ONLY figures+attributed proof, never "claims"/"write nothing else" (=thin copy); 3 mirrors must sync; preserve "STRICT FACTS MODE"/"X"/"Add a quote" literals.
- [Reference-image supply caps](reference-image-supply-caps.md) — page-create photo supply is bounded at TWO stages (per-page DOM `MAX_CONTENT_IMAGES` + mirror `MAX_REFERENCE_PHOTOS`); raise both together; safe for Brand Import (caps at 6 first-N); dedup folds variants independently.
- [URL-sourced facts trust](url-sourced-facts-trust.md) — a successfully-scraped PER-REQUEST source URL makes that generation's facts trusted (relax strict guards in both paths); inspirationUrls don't; +fillDsoCaseStudyNeutralDefaults stops DCA component-default leak.
- Section-bg rhythm avoids all-white stacks via seed+rhythm of light-neutrals: [microsite](microsite-section-bg-rhythm.md) seeds self-section blocks (dandy-columns-v3/testimonial) in mergeWithDefaults; [LP generate-page](lp-section-bg-rhythm.md) seeds at generation only, skips heroes/dso/chrome/full-page, refs SELF_CONTAINED_FULL_PAGE_TYPES lazily (declared later) or use-before-init crash.
- [Generator catalog = drawer read-ACL](generator-catalog-drawer-acl-parity.md) — fetchMediaCatalog must mirror the drawer scope via shared lib/libraryScope.ts or generation starves & repeats one image; scorer only ranks TAGGED images.
- [Shared Dialog clipping + overflow merge](dialog-grid-clip-and-overflow-merge.md) — centered grid DialogContent clips left edge when a wide child (e.g. <select> sized to longest option) blows past max-w; fix at base; overflow-hidden drops base overflow-y-auto via twMerge.
- Image fill strict→AI→last-resort ordering — [scraped relevance gate](scraped-image-relevance-gate.md): strict scraped need score>0 (curated >=0); kill empty-context-word + provenance-meta-tag artifacts · [no relaxed-curated pre-AI](ai-page-offtopic-curated-preempts-aigen.md): never relaxed-fill curated before aiFillEmptyImages (drops the lp-feature topical gate → off-topic photo under a confident AI caption); off-topic imgs fill ONLY in the last-resort pass after AI gen.
- [Generic hero brand-var discipline](generic-hero-brand-vars.md) — brand colors via var()+fallback; dark-theme bg/text overrides, white-on-dark, glass, scrims are OK literals; real leaks = tailwind brand-color utils + brand-tinted rgba glows; panel modal gate must include email submitMode.
- [Case-study blocks real content + approved-only](dso-success-stories-approved-only.md) — all 3 case-study blocks populate from approved Content Library rows with REAL quote/author/stat/image, ranked by locations→segment; empty library keeps built-in examples; approved stats must enter buildApprovedStatSet or get scrubbed to "X".
- [Logo preservation in Replace-imagery](logo-preserve-replace-imagery.md) — logos kept out of AI image pipeline via collectImageSlots filter + separate sanitizeAIImageUrls guard; detection conservative (false-pos keeps a photo, false-neg swaps the logo).
- [Reserved media tags exclude AI reuse](reserved-media-tags-ai-exclusion.md) — logo/team-photo: tag at save route + add to EXCLUDE_TAGS + marker-gated backfill; team block still renders via reconcileTeamMemberPhotos (sources saved rows, not catalog).
- Hand-mirrored block schemas drift (no shared import) — [template-page seeds](template-page-seed-mirroring.md): api-server global seeds mirror lp-studio block defaults, edit BOTH + bump SEED_MARKER · [microsite field parity](microsite-block-schema-field-parity.md): generate-microsite's AI schema string AND its deterministic normalizer must use the EXACT renderer prop field names (dso-comparison used `feature` vs renderer's `need` → empty "Click to edit" left column).
- [Brand-import named token beats wash](brand-import-named-token-over-wash.md) — named --brand/--primary CSS token beats pixel-sampled hero-gradient wash for primary (gated on palette.length>0); var name-matcher must match role keyword as dash-segment ANYWHERE in token.
- [New block-type graduation callsites](block-type-graduation-callsites.md) — ~9 synced callsites to graduate a mockup into a block type; block-variant.ts needs BOTH the import AND the union member, registry ENTRY is separate from createBlock case.
- [mockup-section optional CTA + brand-swap](mockup-section-optional-cta-convention.md) — section mockups: gate CTA on a prop (showCta bool or nullable cta), never `const showCta=true`; theme via token inline-styles, never hardcoded Tailwind color classes/avatar data.
- [thum.io output cap](thumio-output-cap.md) — anonymous gateway caps output ~1280px; width/2400 breaks ratio to a square; max clean 3:2 = viewport/1600x1067/width/1600/crop/800 → 1280×853.
- [dso-case-study field enumeration sync](dso-case-study-field-enumeration.md) — new dso-case-study field needs renderer + PropertyPanel + fillDsoCaseStudyNeutralDefaults + enforceApprovedCaseStudies(strict, both branches); stat-scan & copy-rewrite already recurse.
- [Builder navbar containment](builder-navbar-containment.md) — keep sticky/fixed navbars in the builder canvas via per-block isBuilder→relative AND `[data-lp-builder]{contain:layout;isolation:isolate}`; contain:layout leaves sticky scroll-effects intact.
- [Campaign signal attribution dual-write](signal-attribution-dual-write.md) — microsite/hotlink open/click must dual-write signals row AND email-send row (by hotlinkId); attribute via tenant-scoped resolveContactByEmail (never global); feed vs campaign-cards read different sources.
- BrandLogo tone/contrast on surfaces — [render convention](block-logo-brandlogo-convention.md): hero/nav use BrandLogo + brandHasLogo gate (tone onDark/onLight or brandLogoToneForText), wordmark/decorative marks only when no logo exists · [dark-surface whitening](brand-logo-dark-surface-whitening.md): plain-<img> w/o logoUrlDark force-whitens, mix-blend-difference navs must pass tone="onDark" · [light-surface contrast](brand-logo-light-surface-contrast.md): white logo + no logoUrlDark = white-on-white on light footers, opt-in autoContrast darkens, footers drive tone from actual bg darkness.
- [Marketing SFDC shared OAuth](marketing-sfdc-shared-oauth.md) — LP Settings SF integration shares sales-console sfdc_connections OAuth; form leads sync OAuth-only (client_credentials retired); callback dual-modes on signed returnTo (redirect vs legacy JSON); per-form mappings spread last into createLead.
- [no-dandy-leak-tenant live catalog](no-dandy-leak-tenant-live-catalog.md) — *-tenant variant builds from shared-DB /api/block-catalog (seed never prunes); leaks from excluded dandy-*/dso-*/event-page/spatial-tour rows are pre-existing cleanup debt, not your regression. Don't prune shared prod.
- [Image fill tiering by origin](image-fill-pool-ordering.md) — findBestImage picks best(curated+currentReference scrape) ?? bestScraped(stale) ?? bestStarter; `currentReference` flag (set in buildReferenceFillPool on fresh+current-host scrapes), NOT contentScore>0, is the strict-pass key; tiers beat pool order on cross-origin ties · [own-scrape starvation](image-fill-scraped-tier-starvation.md): a tenant's OWN page-reference scrapes (tag `scraped`) lose the strict pass to any curated >=0 (even a score-0 og screenshot); promote via `tags-'scraped'` (keep refhost/refsrc); EXCLUDE_TAGS includes `logo`.
- [LP block icon dual-field](lp-block-icon-dual-field.md) — block icon fields = one string holding Lucide name OR image URL; render via IconOrImage (lib/icon-value), edit via IconPicker; case-flow uses serializable stage.iconName (not React.ReactNode icon).
- [LP generate-page block selection](lp-generate-page-hero-selection.md) — "same hero/trust-bar/PAS" fix = brand-MATCH all roles (not random); each block under EVERY role; role ORDER from superadmin block_catalog.sort_order (min per role).
- [AI block-catalog filtering line-level](ai-block-catalog-filtering-line-level.md) — ai_enabled prompt filter MUST be line-by-line (clusters share one paragraph); DSO paths filter at the generate-page callsite wrap, not per-builder.
- [Icon-field IconPicker scope](icon-field-scope.md) — IconPicker only on free-string icon fields (perks IS in scope); constrained-union/curated-set fields (dso-problem panels, dso-software-showcase chips) stay specialized; add normalizeIconValue for legacy lowercase rows.
- [Section bg presets](section-surface-bg-presets.md) — give a section block the gradient/brand Background list via 3 edits (interface backgroundStyle?, panel SectionBackgroundControl, renderer resolveSectionSurface + `background` shorthand + surface.base/color/isDark); unset = legacy bgColor, no regression.
- [Section-block premium polish](section-block-premium-polish.md) / [case-study/quote](premium-section-block-polish.md) — section blocks (case-study/quote/testimonial + features/benefits/how-it-works) gate ALL internal polish (Reveal/StatCounter/SectionDecor/motion stagger/gradient tiles) on `isBuilder=!!onFieldChange`; use AccentGlow (surface-aware) not GlowOrbs (dark-only); NO_REVEAL blocks wrap header only; Grid+Bento decor-only (keep bg).
- [fillEmptyImages explicit branches](fillemptyimages-explicit-branches.md) — a new image-bearing array prop needs THREE synced edits in generate-page: collectImageSlots (dedupe) + fillEmptyImages branch (fill, gate on `!x.imageUrl`) + sanitizeAIImageUrls (strip hallucinated host); miss fill→empty, miss sanitize→leak.
- Bulk/batch AI ops — [bulk-classify](library-bulk-ai-classify.md): tenant-scoped, client-driven ≤20 resumable batches, report 429+backoff not fire-and-forget (stops-at-20 bug) · [AI-proxy batch cap](batch-ai-proxy-reclassify.md): per-item batch stalls ~20 (429+maxRetries:2+empty catch); raise retries, throttle, retry-on-429, surface every outcome · [paginated multiselect](paginated-multiselect-bulk-action.md): bulk action uses full selected id set (server-side), never items.filter of loaded page.
- [Welcome/event email duplicate fan-out](welcome-email-workflow-fanout.md) — duplicate welcome emails w/ blank/random names = extra operator workflows on the event (enqueueWorkflowTrigger fans out to ALL), not a template bug; fix = delete the workflows.
- [Block-settings capability gating](block-settings-capability-gating.md) — chrome blocks (nav/header/footer) must be ANCHOR_ONLY in OVERRIDES; BlockRenderer now strips saved settings a block's caps don't support BEFORE wrapWithSettings (caps alone only hide controls, not applied values).
- [AI sales briefing seller direction](ai-sales-briefing-seller-direction.md) — AI account/contact briefings must explicitly frame tenant=SELLER vs account=PROSPECT (via getSalesBrandContext + separate blocks) or the model flips to "account sells to itself".
- [Decorative-panel → real images](how-it-works-alternating-real-images.md) — converting a fake-CSS-panel block to real per-item images needs type+renderer+panel+default AND four generate-page slot callsites (collect/fill/aiFill/sanitize); aiFillEmptyImages is the one most missed.
- [Custom HTML block brand font](custom-html-iframe-brand-font.md) — self-hosted brand font (Bagoss) only renders in the sandboxed Custom HTML iframe if embedded as a base64 data-URI @font-face; app's hashed /assets path + relative URLs don't resolve.
- [Product-detail fill subject-only](product-detail-fill-subject-only.md) — product-grid/dso-products-grid fills must `pick(...,biasPage=false)`; page-vocab bias + the +8 purpose boost lets a crown/logo win a denture card. Hero/feature keep bias.
- Block/brand defaults must stay neutral — [baked accent colors](block-default-baked-accent-colors.md): never hardcode accentColor/gradient literals in createBlock defaults (omit them); dark-surface accent fallback = brand.accentColor (visible), NOT brand.primaryColor (near-black→invisible) · [font @theme default](brand-font-theme-default.md): `--brand-font-*` @theme defaults MUST stay `var(--app-font-*)`; getBrandStyleVars only overrides when a tenant set a font, so a hardcoded face (Bagoss) bleeds onto every fontless tenant.
- [Shared-prod test tenant cleanup](shared-prod-test-tenant-cleanup.md) — integration tests seeding tenants in the shared prod DB must tear down via catalog-discovery cascade (tenantCleanup.ts), not hardcoded child tables; stale-purge guards = slug prefix + exact name + age floor; never swallow FK errors; hooks need 120s timeout.
- [LP draft preview without a session](lp-draft-preview-token.md) — mint an lp_page_reviews token row → /api/lp/preview/:slug?reviewToken=…(&prerender=1 for HTML, &preview=app for SPA); localhost token preview has no brand vars so bg tokens fall back to blue — NOT a regression.
- Footer brand fallback — [Dandy palette leak](footer-dandy-palette-leak.md): non-Dandy footers render Dandy green when AI emits palette literals (#003A30/#C7E738); de-brand to "" → falls back to brand var; guard microsite prompt+normalize AND generate-page deBrandFooterColors · [social links](footer-social-brand-fallback.md): URLs = `props.xUrl||brand.socialUrls.x`, templates ship showSocialLinks:true w/ BLANK urls so render MUST fall back; gate `showSocialLinks!==false && hasUrl`.
- [Scraped-image source-page hero rule](scraped-image-source-page-hero-rule.md) — a scrape is hero-eligible ONLY if it was the source-page hero (candidates[0]→lp-hero); enforce at 3 layers: tag, stale-dedup data, AND selection (findBestImage + validateAndDedupeAIImages hard-gate; soft penalty isn't enough).
- Segment block vocab/pool — [approval expansion](segment-approval-vocab-expansion.md): approved_segments UNIONS (never clamps) AI vocab, fail-closed query, microsite needs prompt+allow-set, generate-page prompt-only · [pool containment](segment-pool-containment-callsites.md): pool-only constraint at 3 post-parse points (clamp + enforceRequiredRoles allowedTypes + zero-usable fallback), NEUTRAL re-leaks · [explicit DSO block requests](explicit-block-requests-dso.md): "vary the mix" drops user-requested DSO blocks; fix = prompt override + deterministic post-normalize inject · [exemplar structure-anchoring](microsite-exemplar-structure-anchoring.md): identical microsite lineups = shared exemplar "match this structure" + per-audience exemplar; fix = exemplars as quality-only + path-dependent temperature (freeform 0.85, fixed 0.7).
- [Media library search ignores tags](media-library-search-tags.md) — `/lp/media/images` free-text `q` matched title only; hash-titled tagged assets were invisible; must OR title ILIKE with a tags-array ILIKE.
- generate-page image pipeline — [scrape gating](generate-page-scrape-gating.md): scrape/mirror ONLY modal URL-box perRequestUrls, NEVER brand inspirationUrls/homepage (re-mirrors dup lp_media every run); brand-import is separate · [collectImageSlots dual role](collectimageslots-restore-vs-fill.md): feeds auto-fill (includeEmpty=false) AND template restore (includeEmpty=true); a manual-only image field must be gated `if(includeEmpty)`, NOT deleted, or restore wipes author imagery · [product library consumer](product-library-blocks-consumer.md): a VALID_TYPES library type w/ NO gen-page consumer falls to the random fill pool; products fixed via fetchProductLibraryItems+enforceProductLibraryBlocks AFTER image-fill, 1:1 prop names, no-op when empty.
- [Brand-import font matching](brand-import-font-matching.md) — AI font-import "stops matching"=2 bugs: script-accent font (Caveat) wins heading via weight>=600, and Typekit hyphen-slugs (adelle-sans) get no Google fallback→Arial; fix in assignRoles + matchFont; renders via stored displayFontUrl.
- [lp-studio dev "Invalid hook call" spin](lp-studio-vite-cache-hook-crash.md) — app shell spins/RouteErrorBoundary while homepage renders fine = stale Vite dep cache serving 2nd React copy; rm node_modules/.vite (artifact+root) + restart web workflow.
- [Critique-pass timeout fail-open](critique-pass-timeout-failopen.md) — banned/cliché copy ships despite detection b/c the corrective rewrite (gpt-4o behind shared semaphore) timed out at 3s and failed open; raised to 12s; it only rewrites ~2 blocks (not page-wide), feeds copyExamples+pillars, never churn buildBrandContext.
- [Nav-dedup both paths](nav-dedup-both-paths.md) — stripRedundantLeadingNav (drop standalone nav before a self-nav hero) must run on BOTH template + freeform generate-page paths; template path skipped it → [nav-header, hero] shipped two navbars; code fix only affects NEW pages.
- [Copilot rewrite_copy instruction threading](copilot-rewrite-instruction-threading.md) — the "Ask AI" rewrite_copy `instruction` must flow through BuilderEditor→refreshBlockCopy→/lp/copy-generate refresh (all 3 layers + sys/user prompt) or an explicit refinement is silently dropped and regenerates the same copy.
- [Preloaded media slug-gating](preloaded-media-slug-gating.md) — hardcoded PRELOADED_VIDEOS in storage.ts bypasses the lp_media sibling ACL; gate Dandy-only media on isProtectedEnterpriseSlug (dandy+dandy-smb), not libraryReadablePredicate or a single slug.
- [Removing a hardcoded image everywhere](blocked-image-removal-surfaces.md) — an unwanted photo id hides in SIX places: starterImages.ts seeds, shared lp_media rows (prod Neon), industryTemplates.ts seeds, live ind-* template pages (DB blocks), AND lp-studio block-registry.tsx defaults; grep the bare photo id repo-wide (excl dist) = 0 before done.
- [Brand-default outline collapse](brand-default-outline-neutral-role-defaults.md) — category-step outlines need shared NEUTRAL_ROLE_DEFAULT_BLOCKS as roleDefaults at BOTH generators (+ the microsite test mirror); empty-pool generic tenants drop uncovered roles else collapse to hero/cta/footer; pool always wins first so no Dandy leak; omits layout/pricing/faq.
- [Create-page Template vs AI paths](create-page-template-vs-ai-paths.md) — "AI didnt customize my template" = clone-path confusion: Template tab copies VERBATIM, only AI Generate rewrites copy; generate-page path is sound (reproduced on TRIOS5 id 272).
