kind = "web"
previewPath = "/"
title = "Landing Page Studio"
version = "1.0.0"
id = "artifacts/lp-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24565

[services.development]
run = "pnpm --filter @workspace/lp-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lp-studio", "run", "build" ]
publicDir = "artifacts/lp-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24565"
BASE_PATH = "/"
